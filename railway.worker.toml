[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build --workspace=apps/backend"

[deploy]
startCommand = "node apps/backend/dist/workers/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
